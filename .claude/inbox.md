- 2026-07-11T15:38: Evaluate stop-slop (https://github.com/hardikpandya/stop-slop/) for integration — decision gated on a blind trial: same text edited under stop-slop vs current writing conventions, results compared without knowing which is which. Evaluation so far (2026-07-11 session): ~half converges with writing-style.md (vague declaratives, binary contrasts, false-agency inventory is new and worth cherry-picking); three rules conflict with Wide-phase spec doctrine (Wh-opener ban vs the When/Behavior/Because formula, rhythm-variation demand vs uniform declaratives, reader-in-the-room vs third-person spec). Working hypothesis: three registers — Wide specs keep current doctrine, Export prose gets stop-slop + hook exemption, plugin-authoring prose uses stop-slop as review checklist. Run together with the character-doctrine A/B (next item): both trials produce a set of writing instructions and test them the same way, so one harness and one side-by-side comparison covers both.
- 2026-07-11T15:38: Character-doctrine fold-in from the causal-character-writing reference is gated on an A/B trial: apply new doctrine (trait-word poisoning, unresolved states, hooks, anchor repetition) vs current doctrine to the same character brief with two independent agents, compare the resulting notes, then decide what to fold into the skills. The two tension calls (plain style vs sensory hooks; section discipline vs anchor repetition) ride on the same trial. See the reference doc's Candidate applications. Run together with the stop-slop trial (previous item) in one combined exercise: produce the instruction sets first, apply each with an independent agent, then compare all results side by side.
- 2026-07-11T15:10: Audit orphan backlog: 16 closed docs (adr/plans/specs/research history) have no inbound links — corpus predates the write-side-linking doctrine; link_notes.py --all finds nothing automatic. Decide whether to backfill links across closed history or accept orphan status for closed docs (research brief↔results provenance edges already added 2026-07-11).
